const initialState = {
    firstName:"",
    lastName: "",
    email:"",
    password:""
}

const signUpFormData = (state=initialState,action) => {
    switch(action.type){
        default:
            return state
    }
}

export default signUpFormData;