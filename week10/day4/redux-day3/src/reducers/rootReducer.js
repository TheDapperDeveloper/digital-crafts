const initialState ={
    firstName: "",
    lastName: "",
    email:"",
    password:""
};

function rootReducer(state = initialState, action) {
    switch (action.type) {
     default:
        return 
    }
}

export default rootReducer